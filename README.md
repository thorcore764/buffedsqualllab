# cli-tool-5cb92c

A Java 17 CLI tool using built-in argument parsing.

## Build & Run

```bash
mvn package
java -jar target/cli-tool-5cb92c-1.0.0.jar run my-target --output ./out
java -jar target/cli-tool-5cb92c-1.0.0.jar status
```

## Project ID

`5cb92c`
